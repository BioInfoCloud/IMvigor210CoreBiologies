% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker_analysis.R
\name{plot_gene_heatmap}

\alias{plot_gene_heatmap}

\title{Row-scaled expression heatmap of a gene set, ordered by response}

\description{
Row-scaled (z-score) heatmap of the supplied genes, with columns ordered by
response status and a response annotation bar.
}


\usage{
plot_gene_heatmap(
  cds,
  genes,
  expr = NULL,
  response_only = FALSE,
  outfile = NULL,
  width = 8,
  dpi = 300,
  verbose = TRUE
)
}

\arguments{
\item{cds}{a \code{DESeqDataSet}.}

\item{genes}{character vector of gene symbols (target first is recommended).}

\item{expr}{optional precomputed matrix from \code{get_tpm_expression}.}

\item{response_only}{logical; if \code{TRUE} keep only response-known samples
(EVAL cohort), otherwise include all samples with \code{Unknown} responses
shown in grey (ALL cohort).}

\item{outfile}{optional output path (\code{png}/\code{pdf}). When omitted the
heatmap is drawn on the current device and the pheatmap object is returned.}

\item{width}{figure width in inches; height auto-scales with gene count.}

\item{dpi}{resolution for a \code{png} \code{outfile}.}

\item{verbose}{logical; print canvas dimensions.}
}

\value{
Invisibly the output path (when \code{outfile} is given) or the
  \pkg{pheatmap} object (otherwise).
}

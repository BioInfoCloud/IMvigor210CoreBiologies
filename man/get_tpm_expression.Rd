% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker_analysis.R
\name{get_tpm_expression}

\alias{get_tpm_expression}

\title{Build a log2(TPM + 1) expression matrix from the IMvigor210 \code{cds}}

\description{
Computes transcripts-per-million (TPM) from raw counts and transcript length,
then applies the same edgeR TMM / CPM low-expression filter used by
\code{filterNvoom}. Returned values are \code{log2(TPM + 1)}.
}


\usage{
get_tpm_expression(
  cds,
  genes = NULL,
  min_cpm = 0.25,
  min_samples = NULL,
  verbose = TRUE
)
}

\arguments{
\item{cds}{a \code{DESeqDataSet} (the bundled \code{cds} object).}

\item{genes}{optional character vector of gene symbols to subset to. Genes
absent after filtering raise an error.}

\item{min_cpm}{minimum counts-per-million for the low-expression filter.}

\item{min_samples}{minimum number of samples that must reach \code{min_cpm};
defaults to one tenth of the sample count (as in \code{filterNvoom}).}

\item{verbose}{logical; print filtering / cohort diagnostics.}
}

\value{
A numeric matrix (genes x samples) of \code{log2(TPM + 1)} values with
  gene symbols as row names.
}
